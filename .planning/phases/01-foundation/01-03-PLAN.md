---
phase: 01-foundation
plan: 03
type: execute
depends_on: ["01-02"]
files_modified: [src/app/(dashboard)/layout.tsx, src/app/(dashboard)/page.tsx, src/components/sidebar.tsx, src/components/user-nav.tsx]
---

<objective>
Create the dashboard layout shell with sidebar navigation and stubbed module sections.

Purpose: Establish the UI structure that all future modules will live within.
Output: Working dashboard layout with sidebar, ready to hold Nano Banana, Gemini Core, and other modules.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

**From CONTEXT.md - Vision:**
- Clean dashboard shell with fixed left sidebar
- Linear-style aesthetic: clean, fast, modern SaaS feel
- Sidebar with icons/labels for major sections (Nano Banana, Gemini Core, etc.)
- Layout ready for all modules without needing redesign

**From CONTEXT.md - Essential:**
- Layout ready for all modules
- Feels like a real product from day 1
- Professional, not prototype

**From CONTEXT.md - Boundaries:**
- No dark mode/theming yet
- Just the shell, not actual feature content

**Modules to stub in sidebar (from PROJECT.md):**
1. Nano Banana (Asset Factory) - icon: sparkles/magic wand
2. Gemini Core (Strategic Brain) - icon: brain/cpu
3. Tracking (Privacy Shield) - icon: shield/chart
4. Campaigns (Dashboard) - icon: layout/grid
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sidebar component with navigation structure</name>
  <files>src/components/sidebar.tsx, src/components/user-nav.tsx</files>
  <action>
Create the sidebar and user navigation components:

1. Create src/components/sidebar.tsx:
   - Fixed left sidebar, full height
   - Width: 240px (or w-60 in Tailwind)
   - Background: slate-50 or similar light neutral
   - Border-right for separation

   Structure (top to bottom):
   - Logo/app name at top ("AdOrchestrator" text logo)
   - Navigation section with module links:
     * Nano Banana (sparkles emoji or icon placeholder) - links to /nano-banana (disabled/coming soon state)
     * Gemini Core (brain emoji or icon placeholder) - links to /gemini-core (disabled/coming soon state)
     * Tracking (shield emoji or icon placeholder) - links to /tracking (disabled/coming soon state)
     * Campaigns (grid emoji or icon placeholder) - links to /campaigns (disabled/coming soon state)
   - All nav items show "Coming Soon" tooltip or badge except Dashboard/Home
   - Active state styling (left border accent, background change)
   - UserNav component at bottom

   Style: Linear-inspired
   - Clean sans-serif text (system font stack is fine)
   - Subtle hover states (background shade change)
   - 2px left border accent for active item (blue or accent color)
   - Icons can be emoji initially (🍌 ⚡ 🛡️ 📊) - we'll add proper icons later

2. Create src/components/user-nav.tsx:
   - Shows current user email (from Supabase auth)
   - Logout button/link
   - Position at bottom of sidebar
   - Get user from Supabase server client
   - Logout action calls Supabase signOut and redirects to /login

Keep it simple - no dropdown menus, just email display and logout.
Use Server Components where possible, Client Components only for interactivity.
  </action>
  <verify>Components render without errors, TypeScript compiles, logout action defined</verify>
  <done>Sidebar shows navigation with stubbed modules, user email displays, logout works</done>
</task>

<task type="auto">
  <name>Task 2: Create dashboard layout with route group and home page</name>
  <files>src/app/(dashboard)/layout.tsx, src/app/(dashboard)/page.tsx, src/app/page.tsx</files>
  <action>
Set up the dashboard layout using Next.js route groups:

1. Create src/app/(dashboard)/layout.tsx:
   - Route group layout (parentheses don't affect URL)
   - Flex layout: sidebar fixed left, content area fills remaining space
   - Sidebar component on left
   - Main content area with padding
   - Min-height: screen height
   - Content area should have max-width for readability (max-w-6xl or similar)
   - Light background for content area (white or slate-50)

2. Create src/app/(dashboard)/page.tsx (this becomes the / route for authenticated users):
   - Welcome message: "Welcome to AdOrchestrator"
   - Brief explanation of what each module does (placeholder cards or list)
   - "Get started by uploading brand guidelines in Nano Banana" or similar guidance
   - Clean, centered content
   - Should feel like a real dashboard home, not "coming soon" everywhere

3. Update src/app/page.tsx:
   - This should redirect to /(dashboard) or you can remove it and let (dashboard)/page.tsx be the home
   - The route group means (dashboard)/page.tsx IS the / route
   - Just ensure / works correctly for authenticated users

Layout requirements:
- Sidebar stays fixed on scroll
- Content area scrolls independently
- Professional spacing (p-8 or similar in content area)
- Should work at common screen sizes (1024px+, no mobile optimization needed yet)
  </action>
  <verify>npm run dev shows dashboard layout at /, sidebar visible, content area renders, navigation items display</verify>
  <done>Dashboard layout works with sidebar and content area, home page shows welcome content, looks professional</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Dashboard layout renders at / (when authenticated)
- [ ] Sidebar shows all module navigation items
- [ ] User email displays in sidebar
- [ ] Logout button works (redirects to /login)
- [ ] Layout looks professional (Linear-style aesthetic)
- [ ] No TypeScript or ESLint errors
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Layout ready for future module development
- Looks like a real product, not a prototype
- Phase 1 Foundation complete
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`

Commit guidance:
- Task 1: `feat(01-03): create sidebar with navigation structure`
- Task 2: `feat(01-03): create dashboard layout and home page`
- Metadata: `docs(01-03): complete layout shell plan`

Note: This is the final plan for Phase 1. After SUMMARY.md, update ROADMAP.md to mark Phase 1 complete.
</output>
